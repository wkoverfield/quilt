node_modules/
dist/
*.log
.DS_Store

# Intermediate halves of the split-screen contrast GIF (we commit examples/contrast.gif)
examples/contrast-without.gif
examples/contrast-with.gif

# Quilt's own local state (also self-ignored via .quilt/.gitignore)
.quilt/

# Local artifacts from dogfooding `quilt setup` on this repo — keep out of the public tree
.mcp.json
.claude/
CLAUDE.md
